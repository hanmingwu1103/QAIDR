# Default UMAP configuration for interval UMAP

Returns a list of UMAP hyperparameters with default settings.

## Usage

``` r
umap_config_default()
```

## Value

A named list of UMAP configuration parameters.

## Examples

``` r
cfg <- umap_config_default()
```
