# Strong UMAP configuration for interval UMAP

Returns a list of UMAP hyperparameters with stronger optimization
settings, emphasizing local structure preservation.

## Usage

``` r
umap_config_strong()
```

## Value

A named list of UMAP configuration parameters.

## Examples

``` r
cfg <- umap_config_strong()
```
