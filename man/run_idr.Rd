% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr-methods.R
\name{run_idr}
\alias{run_idr}
\title{Run interval dimensionality reduction methods}
\usage{
run_idr(
  x,
  methods = c("C-PCA", "V-PCA", "MR-PCA", "SPCA", "IMDS", "Int-UMAP"),
  labels = NULL,
  no_dim = 2,
  umap_config = umap_config_default(),
  verbose = TRUE
)
}
\arguments{
\item{x}{An \code{interval_data} object (should be standardized).}

\item{methods}{Character vector of methods to run. Default includes all six:
\code{"C-PCA"}, \code{"V-PCA"}, \code{"MR-PCA"}, \code{"SPCA"},
\code{"IMDS"}, \code{"Int-UMAP"}.}

\item{labels}{Optional factor of class labels. If \code{NULL}, uses
\code{x$labels}.}

\item{no_dim}{Integer number of target dimensions (default 2).}

\item{umap_config}{List of UMAP hyperparameters. Default uses
\code{umap_config_default()}.}

\item{verbose}{Logical; print progress messages (default \code{TRUE}).}
}
\value{
An object of class \code{idr_projections}: a named list where each
element contains \code{C} (centers matrix), \code{R} (radii matrix), and
\code{type} ("Point" or "Interval").
}
\description{
Applies multiple interval DR methods (C-PCA, V-PCA, MR-PCA, SPCA, IMDS,
Int-UMAP) to an \code{interval_data} object and returns the projections.
}
\examples{
\dontrun{
data(cars_mm)
x <- standardize(cars_mm)
proj <- run_idr(x)
}
}
