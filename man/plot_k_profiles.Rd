% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_k_profiles}
\alias{plot_k_profiles}
\title{Plot quality/behavior index profiles over K}
\usage{
plot_k_profiles(profile_data, metric = NULL)
}
\arguments{
\item{profile_data}{A data frame as returned by \code{k_profiles()}.}

\item{metric}{Character string specifying which distance metric to plot.
If \code{NULL}, returns a list of plots for all metrics.}
}
\value{
A \code{ggplot} (or list of plots if \code{metric} is \code{NULL}).
}
\description{
Creates a 2 x 3 grid of line plots (Quality and Behavior for T&C, MRRE,
LCMC) for a given distance metric.
}
\examples{
\dontrun{
data(cars_mm)
x <- standardize(cars_mm)
proj <- run_idr(x)
profiles <- k_profiles(x, proj, K_max = 10)
plot_k_profiles(profiles, metric = "Wasserstein")
}
}
